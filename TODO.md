TODO:
 1. When clicking onto a repo name, nothing happens when I expected to see a list of all the analysis.
 2. The analysis/badge associated to each repo is the latest analysis ran when it should be an average of all the latest workflows analysis
 3. Improve docker compose files to watch and reload frontend when in dev mode
 4. Remove social network links to FastAPI accounts
 5. Add org/repo level AI preferences (provider and model)
 6. Detect which providers and models are available based on environment
 7. Setup default model per provider based on environment
 8. Follow PR status for each repo in a new tab
 9. Update usage in billing tab
 10. Add landing page with dedicated privacy notice and ToS pages. Also add pricing information
 11. Open Github App install/settings in a popup. Close the popup once install/updates are done instead of redirecting to repos.
 12. Fix pre-commit hooks
